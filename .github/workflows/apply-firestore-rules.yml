name: Apply Firestore Security Rules

on:
  workflow_dispatch:

jobs:
  apply-rules:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '20'

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Save Service Account Key
        env:
          SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: |
          echo "$SERVICE_ACCOUNT_JSON" > /tmp/service-account.json

      - name: Apply Firestore Rules
        env:
          GOOGLE_APPLICATION_CREDENTIALS: /tmp/service-account.json
        run: |
          # Criar arquivo firebase.json para deploy
          echo '{"firestore":{"rules":"config/firestore_rules.txt"}}' > firebase.json

          # Login com service account
          export GOOGLE_APPLICATION_CREDENTIALS=/tmp/service-account.json

          # Deploy das regras
          firebase deploy --only firestore:rules --project agendafacil-pro

          echo "✅ Regras de segurança aplicadas!"
